apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library-claim 
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi 
  storageClassName: local-path